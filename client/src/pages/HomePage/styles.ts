import {makeStyles} from '@material-ui/core/styles';

export const useStyles = makeStyles(theme => ({
    greating: {
        display: 'flex',
        flexDirection: 'row',
        alignItems: 'center',
    },
}));
